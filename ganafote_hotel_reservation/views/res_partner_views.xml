<?xml version="1.0" encoding="UTF-8" ?>
<odoo>

    <record id="view_partner_form_inherit_ganafote" model="ir.ui.view">
        <field name="name">res.partner.form.inherit.ganafote</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form" />
        <field name="arch" type="xml">
            <xpath expr="//field[@name='category_id']" position="after">
                <field name="is_hotel" />
            </xpath>
        </field>
    </record>

    <record id="view_res_partner_filter_inherit_ganafote" model="ir.ui.view">
        <field name="name">res.partner.select.inherit.ganafote</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_res_partner_filter" />
        <field name="arch" type="xml">
            <xpath expr="//filter[@name='inactive']" position="before">
                <filter
                    string="Hotels"
                    name="is_hotel"
                    domain="[('is_hotel', '=', True)]"
                />
                <separator />
            </xpath>
        </field>
    </record>

</odoo>

